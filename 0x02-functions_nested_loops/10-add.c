#include "main.h"

/**
 * add - adds two numbers
 * int a is the first number to be added while int b, the second
 * @a: first number to add
 * @: second number to be aded
 * Return: return th sum
 */
int add(int, int)
{
	int a, b, c;

	c = a + b;
	_putchar(c);
	return (c);
}
