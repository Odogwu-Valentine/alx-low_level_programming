#include <stdio.h>

/**
 * main - Entry point
 * Description: Printing alphabets in all cases
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int A;

	for (A = 65; A <= 122; A++)
	{
		putchar(A);
	}
putchar('\n');
return (0);
}
