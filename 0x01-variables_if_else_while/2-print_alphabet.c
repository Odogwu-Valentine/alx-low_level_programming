#include <stdio.h>

/**
 * main - Entry point
 * Description : playing the alphabet game
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int A;

	for (A = 1 ; A <= 9 ; A++)
	{
		putchar (A);
	}
return (0);
}
