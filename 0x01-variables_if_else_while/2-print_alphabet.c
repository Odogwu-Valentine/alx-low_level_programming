#include <stdio.h>

/**
 * main - Entry point
 * Description : playing the alphabet game
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int A;

	for (A = 97 ; A <= 122 ; A++)
	{
		putchar (A);
		putchar ('\n');
	}
return (0);
}
